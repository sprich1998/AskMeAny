# Scripts

Dev and ops helper scripts (not deployed in containers).

## Planned scripts

| Script | Purpose |
|--------|---------|
| `dev-up.sh` | `docker compose up` wrapper |
| `dev-down.sh` | Tear down stack |
| `migrate.sh` | Run Postgres migrations |
| `seed-demo.sh` | Optional demo session data |

Keep scripts POSIX-friendly and callable from repo root.
