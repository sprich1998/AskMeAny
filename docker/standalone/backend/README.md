# Standalone: `backend`

**Build context:** repo root (or `apps/backend`)

**Port:** `4000`

**Planned files:** `Dockerfile`, `.env.example`

**Depends on:** `postgres`, `redis`, `qdrant`; optional `browser-worker` URL.
