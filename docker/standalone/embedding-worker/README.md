# Standalone: `embedding-worker`

**Build context:** repo root (or `apps/embedding-worker`)

**Planned files:** `Dockerfile`, `.env.example`

**Depends on:** `redis` (BullMQ), `postgres` (read actions), `qdrant` (vectors).
