# Standalone: `frontend`

**Build context:** repo root (or `apps/frontend`)

**Port:** `3000`

**Planned files:** `Dockerfile`, `.env.example`

**Depends on:** `backend` (API URL), noVNC/WebSocket endpoints for browser stream.
