# `src/db`

Postgres access layer.

Tables: `browser_sessions`, `page_snapshots`, `actions`, `network_events`, `dom_mutations`, `intents`, `workflows`, `workflow_steps`.

Migrations live in [`infra/postgres/migrations/`](../../../infra/postgres/migrations/).
